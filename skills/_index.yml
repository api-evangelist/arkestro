generated: '2026-08-06'
method: generated
source: openapi/arkestro-api-v2-openapi.yml
note: >-
  Arkestro publishes no AGENTS.md and no skill files of its own; these are generated by API
  Evangelist from the public OpenAPI 3.1.1 contract and the webhook trait inside it. Every
  step is grounded in a real operation. Because the published spec declares NO operationIds
  on any of its 46 operations, steps address operations by HTTP method and path rather than
  by id — see overlays/arkestro-api-v2-overlay.yaml for proposed operationIds, which are
  API Evangelist's and are not served by Arkestro.
skills:
- file: arkestro-run-a-sourcing-event.md
  name: Run an Arkestro sourcing event
  description: >-
    Create a sourcing event, attach required supplier documents, poll document and quote
    submissions, and read the award.
  api: openapi/arkestro-api-v2-openapi.yml
  operations:
  - POST /api/v2/events
  - GET /api/v2/events
  - GET /api/v2/events/{id}
  - PATCH /api/v2/events/{id}
  - POST /api/v2/events/{event_id}/documents
  - GET /api/v2/events/{event_id}/documents
  - GET /api/v2/events/{event_id}/document_submissions
  - GET /api/v2/events/{id}/schedule
  - GET /api/v2/quote_submissions
  - GET /api/v2/quote_submissions/{id}
  - GET /api/v2/events/{id}/award
- file: arkestro-sync-suppliers-and-spend-catalog.md
  name: Sync suppliers and the spend catalog into Arkestro
  description: >-
    Upsert supplier organizations, contacts, categories, items and purchase orders from an
    upstream master using external_id, with the delete asymmetry called out.
  api: openapi/arkestro-api-v2-openapi.yml
  operations:
  - GET /api/v2/supplier_organizations
  - POST /api/v2/supplier_organizations
  - PATCH /api/v2/supplier_organizations/{id}
  - GET /api/v2/supplier_contacts
  - POST /api/v2/supplier_contacts
  - PATCH /api/v2/supplier_contacts/{id}
  - DELETE /api/v2/supplier_contacts/{id}
  - GET /api/v2/corporate/categories
  - POST /api/v2/corporate/categories
  - GET /api/v2/corporate/items
  - POST /api/v2/corporate/items
  - GET /api/v2/corporate/purchase_orders
  - POST /api/v2/corporate/purchase_orders
- file: arkestro-pull-event-analytics.md
  name: Pull Arkestro event analytics incrementally
  description: >-
    Read the four dbt-built analytics marts and poll them with refreshed_after instead of
    re-reading everything.
  api: openapi/arkestro-api-v2-openapi.yml
  operations:
  - GET /api/v2/event_analytics/buyer_leaderboards
  - GET /api/v2/event_analytics/quotes
  - GET /api/v2/event_analytics/supplier_invitation_statuses
  - GET /api/v2/event_analytics/survey_results
  - POST /api/v2/event_analytics/metrics/export_task
  - POST /api/v2/event_analytics/rounds/export_task
- file: arkestro-verify-webhooks.md
  name: Verify and handle Arkestro webhooks
  description: >-
    Verify the HMAC-SHA256 signature, survive secret rotation, deduplicate on
    X-Arkestro-Idempotency-Key, and respond within the 10-second delivery timeout.
  api: openapi/arkestro-api-v2-openapi.yml
  source: asyncapi/arkestro-webhooks.yml
  operations: []
